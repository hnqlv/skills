interface:
  display_name: "Define Spec"
  short_description: "Turn requirements into an approved spec"
  default_prompt: "Use $define-spec to turn this feature idea into a concise SPEC.md before implementation."
